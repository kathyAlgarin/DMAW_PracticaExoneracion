@model List<Producto>

@{
    ViewData["Title"] = ViewData["Titulo"]?.ToString();
}

<h1>@ViewData["Titulo"]</h1>
<p>Tienda: @ViewBag.NombreTienda</p>
<p>Total de productos: @ViewBag.TotalProductos</p>

@foreach (var producto in Model)
{
    <p>@producto.Nombre</p>
}
